# Copie para .env e preencha os valores (não faça commit do .env).
# cp .env.example .env

# Banco de dados PostgreSQL
DB_POSTGRES_HOST=127.0.0.1
DB_POSTGRES_PORT=5432
DB_POSTGRES_USER=postgres
DB_POSTGRES_PASSWORD=postgres
DB_POSTGRES_NAME=desafio_wellbe

# Segurança do Django
DEBUG=True
SECRET_KEY=desafio_wellbe_super_chave_secreta
